# Records

This folder stores proof-run evidence and completed run artifacts for `parameter-golf-uniform-int4`.

- Keep reusable runbooks and launch helpers at the repo top level.
- Save completed proof logs, notebooks, and summaries here so RunPod evidence survives beyond the pod lifetime.
